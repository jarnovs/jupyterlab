services:
  jupyter:
    build:
      context: .
    container_name: jupyter
    networks:
      - jupynet
    volumes:
      - data:/anaconda

  nginx:
    image: nginx:latest
    container_name: junginx
    ports:
      - 8888:8888
    volumes: 
      - ./default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - jupynet

networks:
  jupynet:

volumes:
  data:
